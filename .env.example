# Copy to ~/.dev.env (or source locally). The deploy reads the token from a Modal
# secret named zod-stt-token; the client reads these.
STT_MODAL_URL=https://cronus--zod-stt-stt-web.modal.run
STT_MODAL_TOKEN=replace-with-openssl-rand-hex-24
